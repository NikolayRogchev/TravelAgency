@model IEnumerable<TripListingServiceModel>
@{
    ViewData["Title"] = "All Available Trips";
}

@if (Model.Count() != 0)
{
    <h2>All Available Trips</h2>
    Html.RenderPartial("_TripListingPartial", Model);
}
else
{
    <h2>Currently there are no available trips</h2>
}